#!/bin/bash

# Install tiny HTTP server (globally)
# NOTE: Uncomment and run one, if ygitou don't have it installed already
#sudo npm install http-server -g

# Run local HTTP server from current directory
http-server .